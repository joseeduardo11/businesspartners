# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: '2.4'
metadata:
  name: 'businesspartners'
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          destination: Gateway_Sandbox
          url: https://zzz-sl-sap034.zeppelin.corp:443
        credentials:
          username: env:HANA_USERNAME
          password: env:HANA_PASSWORD
        app:
          name: ZBUSINESSPARTNER
          description: "YFIORI"
          package: "zfiori"
          transport: "G2XK900059"
