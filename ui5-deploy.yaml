# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: '2.4'
metadata:
  name: 'businesspartners'
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          auth: basic
          client: 100
          url: http://zzz-sl-sap034.zeppelin.corp/
        credentials:
          username: env:HANA_USERNAME
          password: env:HANA_PASSWORD
        app:
          name: ZBUSINESSPARTNER
          description: "YFIORI"
          package: "zfiori"
          transport: "G2XK900059"
